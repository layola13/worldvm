# 20 附录：编码表与模板

## 1. 目标
提供可直接复用的编码约定、模板和样例，减少实现歧义。

## 2. 颜色编码表（10色）
| 值 | 颜色 | 语义示例 |
|---:|---|---|
| 0 | 黑 | 阻断、吸收、未知 |
| 1 | 白 | 高反射、高亮、可见 |
| 2 | 灰 | 中性、稳定 |
| 3 | 红 | 高热、警示、危险 |
| 4 | 橙 | 过渡热态 |
| 5 | 黄 | 强提示、高关注 |
| 6 | 绿 | 安全、生长、可通行 |
| 7 | 蓝 | 冷态、液体关联 |
| 8 | 靛 | 深冷/深层状态 |
| 9 | 紫 | 异常/高复杂状态 |

## 3. 材料枚举表示例
| 值 | 材料 |
|---:|---|
| 0 | SOLID |
| 1 | LIQUID |
| 2 | GAS |
| 3 | FRAGILE |
| 4 | ELASTIC |
| 5 | COMPOSITE |

## 4. 指令模板示例
```text
INSTR:
  opcode: MOVE
  actor:  hand_left
  target: cup
  magnitude: 1
  duration: 2
  flags: requires_focus|safety_checked
```

## 5. 计划模板示例（YAML 伪格式）
```yaml
plan_id: make_sandwich
nodes:
  - id: 1
    op: GRAB
    actor: left_hand
    target: bread
  - id: 2
    op: GRAB
    actor: right_hand
    target: knife
  - id: 3
    op: SPREAD
    actor: right_hand
    target: bread
    preds: [1,2]
  - id: 4
    op: CONFIRM
    actor: self
    target: done
    preds: [3]
```

## 6. 情绪规则模板
```text
RULE fear_gate:
  if arousal > 220 and control < 40:
    allow_ops = [AVOID, FREEZE, ASK]
    deny_ops  = [APPROACH, BIND]
```

## 7. 事件日志模板
```json
{
  "event_id": 1024,
  "tick": 17,
  "scene_id": 9,
  "ops": ["FOCUS", "MOVE", "GRAB"],
  "collisions": 1,
  "delta_bytes": 84,
  "reason_code": "E_OK"
}
```

## 8. 解释链模板
```text
INPUT -> PARSE -> IR -> PLAN -> EXEC(TICKS) -> DELTA -> OUTPUT
```

## 9. 关系边模板
```text
subject_id, relation_type, object_id, weight, confidence, source_id, ts
```

## 10. Reason Code 统一编码表（补充）
| 代码 | 域 | 含义 |
|---|---|---|
| `E_OK` | core | 执行通过 |
| `E_SCHEMA_INVALID` | core | 输入结构不合法 |
| `E_POLICY_BLOCKED` | policy | 被策略守卫拦截 |
| `P_COLLISION_VIOLATION` | physics | 物理约束冲突 |
| `C_REACTION_UNSAFE` | chemical | 化学反应风险过高 |
| `T_EXISTENCE_VIOLATION` | temporal | 实体在事件时刻不存在 |
| `T_ORDER_CONFLICT` | temporal | 事件先后顺序冲突 |
| `T_CAUSAL_CONFLICT` | temporal | 因果方向冲突 |
| `T_ROLE_INACTIVE` | temporal | 角色在事件时刻未生效 |

## 11. 调试清单模板
1. 是否命中正确模板？
2. 是否进入正确焦点区？
3. 冲突是否按优先级处理？
4. 提交 Delta 是否符合预期？
5. 解释链是否完整？

## 12. ROM 段表示例
| 段名 | 偏移 | 大小 |
|---|---:|---:|
| header | 0 | 64B |
| lexicon | 64 | 3MB |
| plans | ... | 2MB |
| microcode | ... | 1.5MB |
| affect_rules | ... | 1MB |
| seed_graph | ... | 1.5MB |
| reserved | ... | 1MB |

## 13. 场景案例模板
```text
case_name: hammer_glass_break
entities: [hammer, glass, table]
focus: glass
expect:
  - collision_count >= 1
  - glass_state == BROKEN
  - delta_contains("spawn_fragments")
```

## 14. 本章结论
附录模板用于“统一表达”。  
项目越早统一模板，后续跨模块协作成本越低。

